version: '3.8'

services:
  news-pulse-bot:
    build: .
    container_name: news-pulse-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
